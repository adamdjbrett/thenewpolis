<nav class="navbar navbar-expand-lg bg-white border-dark border-bottom desktop p-3">
  <div class="container">
    <a class="navbar-brand" href="{{wordpress.config.url}}">
	<img class="img-fluid rounded-3 me-2" width="35" height="35" alt="{{wordpress.config.title}}" 
src="{{wordpress.config.navbar_logo}}"/>
  {{wordpress.config.navbar_title}}</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">      
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
        {% for key, item in wordpress.config.navlist %}<li class="nav-item">
          <a class="nav-link nav-link-maroon-hover" aria-current="page" href="{{item.url}}">{{item.nav}}</a>
        </li>{% endfor %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle " href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            {{wordpress.config.navbar_dropdown_title}}
          </a>
          <ul class="dropdown-menu">{% for key, item in wordpress.config.navdropdown_list %}
            <li><a class="dropdown-item" href="{{item.url}}">{{item.nav}}</a></li>{% endfor %}
          </ul>
        </li>
      </ul>      
<div class="d-flex" role="search">
<button type="button" class="btn btn-outline-dark" data-bs-toggle="modal" 
data-bs-target="#searchModal">
<i class="{{wordpress.config.widget_search_icon}}"></i></button>
        </a>
      </div>
    </div>
  </div>
</nav>